# 브랜치·PR 컨벤션

최근 PR들에서 브랜치 스택 관련 문제가 반복되어, 이후 작업에 아래 규칙을 적용합니다.
(Claude로 작업 중이라면 이 규칙을 그대로 따라 브랜치를 나눠주세요.)

## 규칙 1 — 새 브랜치는 항상 최신 main에서 분기

```
git checkout main && git pull origin main
git checkout -b feat/<기능명>
```

이전 작업 브랜치나 리뷰 대기 중인 브랜치 위에 새 브랜치를 쌓지 마세요.
스택 위에서 만든 PR은 머지 시 하위 브랜치의 미승인 커밋까지 main에 함께 들어갑니다.

## 규칙 2 — 리뷰 대기/보류 중인 브랜치 위에 쌓지 않기

현재 대기 중(main 미포함):
- `#137` 챗봇 (비용 상한·인젝션 방어 조건 대기)
- `#154` 로그인 (서버 저장 방향 논의 대기)

이 브랜치들 위에 새 작업을 쌓으면, 새 PR 머지 시 대기 기능이 우회 병합됩니다.
새 작업은 반드시 main 기준으로 분기하세요. (이 목록은 상태가 바뀌면 갱신합니다.)

## 규칙 3 — PR 하나에 기능 하나 (스택 금지)

여러 기능은 각각 별도 브랜치·별도 PR로. 의존성이 꼭 필요하면 PR 설명에 "#NN 위에 쌓임"을 명시하세요.

## 규칙 4 — i18n.ts 새 사전 블록은 파일 끝에 추가

`export function t(` 위에 넣지 말고 파일 맨 끝에 append 하세요. 삽입 지점이 겹치면 매 머지마다 충돌납니다.

## 규칙 5 — 머지 전 base 확인

```
gh pr view <번호> --json baseRefName
```

base가 main인지 확인하세요. main이 아니면 리베이스하거나 콘텐츠만 체리픽하세요.

## 왜 중요한가

발표 없는 URL 심사라 main이 항상 배포 가능한 상태여야 합니다. 스택 꼬임으로
대기 중인 기능(챗봇 등)이 몰래 들어가면 심사 중에 문제가 될 수 있습니다.
