# 다음 할 일 정리 (2026-07-25 교수님 피드백 반영)

> 우선순위는 교수님 상담 피드백 순서를 따름. 각 항목에 상태와 관련 파일을 표시.
> 상태: ⬜ 미착수 / 🔶 문서화만 완료(실행 전) / ✅ 완료

> **[2026-07-26 우선순위 재확인]** 골든데이터셋 확보(우선순위 1)는 목표치 도달로 사실상 완료.
> **지금부터는 우선순위 2(Judge 신뢰성 검증/Train-Val-Test 실제 적용/사람 평가)에 집중** —
> 교수님 피드백("시스템 완성보다 검증 우선")에 맞춰 **우선순위 6(전세사기 구조 위험 확장:
> 실거래가 API·등기부등본 파싱 등)은 명시적으로 보류**. 우선순위 3(프론트엔드)·4(백엔드)도
> 검증이 끝나기 전까지는 착수하지 않음.

---

## 우선순위 1 — 골드데이터셋 확보 (최우선)

- ✅ **AI Hub 71834("계약 법률 문서 서식 데이터") 다운로드 완료** — 부동산임대(346)/표준약관(400)/금전거래(537)/대리점(152)/고용계약(513) = **1,948건**을 `data/raw/aihub_contract_forms/`에 텍스트로 추출, `parser.py` 호환 확인 완료.
- 🔶 **AI Hub 71610 신청은 진행하는 것으로 결정** (팀 논의 1 확정) — 보안서약서 제출은 사람이 직접 해야 함, 미착수
- ✅ **AI Hub 부동산임대 카테고리 전수 검토 완료** — 271건 후보 중 부동산임대 58건(15개 템플릿 계열)을 유사도로 압축해 **계열 대표 15개 전부 확인**, 상업용부동산임대계약서 0193/0190/0297/0079/0011 등을 조항 단위로 라벨링 → `data/real_labels_aihub_extension.md`. 최고 강도 사례(0079, "이의제기금지" 5회 반복), 명도지연배상 150~200% 교차검증 벤치마크(LBox 판례+AI Hub 템플릿 3개 소스 일치), 균형잡힌 대조 템플릿 2건(0297/0011) 확보. ⚠️ 정부/법원 판정 아닌 자체 판단 기준. 나머지 카테고리(표준약관 126건/고용계약 64건/대리점 17건)는 도메인이 다르거나 오염 심해 스코프 밖으로 확정, 금전소비대차 6건만 향후 검토 후보로 남김
- ✅ **LBox Open "제N조" 조항 후보 138건 전수 검토 완료** — **9건 정식 라벨링** → `data/real_labels_lbox_extension.md`: 신탁특약 대조쌍(id=2488/62108), 갱신청구권 포기 대조쌍(id=37441), 법원이 **정면으로 약관법 위반 무효 판정**한 사례(id=90237, 조항 5개), 강행규정/임의규정 구분 대조쌍(id=5882), 갱신거절사유 제한(id=11329), 위약금 기준선(id=146290), 즉시해지 유효사례(id=116067), 주택/상가 기준 차이(id=78728). 나머지 129건 전부 검토했으나 스코프 밖(우선변제권 계산/신탁 대항력/회생파산/형사/권리금 등)이거나 기존 패턴과 중복 확인, 추가 라벨링 대상 없음
- ✅ **신규 risk_type "신탁관계·소유권 불안정 고지" 정식 채택 완료** — `analysis.txt`/`baseline.txt`에 6번째 유형으로 반영(2026-07-26). 근거: `docs/jeonse_fraud_causes_research.md`(전세사기 원인 리서치) + LBox id=2488/62108 실증사례
- ✅ **구조적 위험 체크리스트 추가** — 전세사기의 대부분(70~80%+)은 계약서 조항이 아니라 임대인 재무상태·시세조작·이중계약 등 계약서 밖 요인에서 발생한다는 리서치 결론에 따라, 조항 판정과 별개로 항상 노출되는 고정 체크리스트(등기부등본 재확인·실거래가 대조·대항력 확보 시점·신탁여부 확인·HUG 보증 가입여부)를 Ours 파이프라인에 추가(`analysis.py`). eval.py 지표에서는 제외 처리해 기존 리콜/정밀도 수치 보존
- ⬜ **한국공정거래조정원(fairnet.kofair.or.kr) 수동 검색** — 자동화가 안 먹혀서 사람이 직접 "임대차/전세/보증금" 검색해야 함
- 🔶 **전세사기 데이터 사용은 확정** (팀 논의 2 확정) — risk_type 확장 필요성까지 실증 근거로 확인됨(위 신탁 케이스), 정식 채택은 팀 회의 필요
- ✅ **hldcc 중복 사례 정리** — 98건 전수 스캔(정확 금액 매칭) 결과 id=2396/1259 1쌍만 완전동일 사건으로 확인, 삭제 대신 `_index.csv`에 "Train/Val/Test 분할시 같은 셋에 배정" 메모 추가. 그 외 중복 없음(070 카테고리 상호 유사도 높은 건들은 같은 유형의 별개 사건, 중복 아님)
- ✅ Test셋 최소 30건 확보 목표치 — 정부/법원 판정 기반 확정 라벨: real_labels.md 4문서 + hldcc 12건 + LBox 9건(조항 단위로는 90237의 5개 포함 약 15개) = **문서/사건 기준 25건, 조항 기준 30건 이상**으로 목표치 도달. AI Hub 자연발생 라벨(신뢰도 낮은 별도 등급) 약 15개 이상 추가 확보. 3.4절(`docs/clause_level_dataset_and_split.md`)에 최종 집계 반영은 다음 착수
- ⬜ **git 관리 결정**: LBox 필터링 파일 43MB, 지금 `.gitignore` 안 걸림 — git-lfs로 갈지, gitignore 처리하고 팀 로컬/외부 스토리지 공유로 갈지 정해야 함

## 우선순위 2 — 관찰1(Judge 평균만으론 우열 판별 어려움) 보완

**Judge 신뢰성 코드 수정**
- ✅ `judge.py`가 rationale을 `JudgeScores.rationale`에 저장 (state.py에 필드 추가, `graph.py`/`eval.py`의 평균 계산은 `judge_score_avg()` 헬퍼로 분리해 rationale 필드가 섞이지 않도록 처리)
- ✅ `judge.py`의 aspect 순서를 매 호출마다 `random.shuffle`
- ✅ `judge_rubric.txt`에 aspect별 1/3/5점 앵커 예시 추가 — 가상 예시가 아니라 **실제 골든데이터**(hldcc id=20/37, AI Hub 상가임대차 서식) 기반
- ✅ **비교/선호 판단 트랙 신설** (`docs/human_llm_judge_agreement_design.md` 11절) — `judge_pairwise.py`/`judge_pairwise_rubric.txt` 구현 완료, 위치편향 통제(`compare_debiased`)까지 포함

**Train/Val/Test 실제 적용**
- 🔶 분리안 문서화 완료 (`docs/clause_level_dataset_and_split.md`) — 실제 코드/실험에 반영은 안 됨
- ⬜ Train 6문서로 기존 Full/Baseline/w/o-persona 결과 재료 삼아 persona prompt 개선판 작성
- ⬜ Val 3문서(정상예금약관24조+즉시연금2018-8+국토부표준계약서)로 개선판 선택
- ⬜ 확정판을 Test(신규 데이터)에 **1회만** 실행해 최종 수치 확정

**정밀 평가 인프라**
- ✅ 조항 단위 라벨 66개 확보 (`data/clause_level_labels.csv`, 자작 5문서 기준)
- ✅ `eval.py`에 조항 단위 정밀 recall/precision 추가 완료 — `parser.split_clauses()`의 `clause_id`(`clause_NNN`, 결정적)를 `clause_level_labels.csv`와 직접 매칭해 TP/FP/FN/TN 계산, `docs/eval_clause_precision.md`로 저장(`render_clause_precision_markdown`). 기존 문서 단위 근사 리콜은 하위호환용으로 유지(교체가 아니라 병기)
- ✅ **`data/real_clause_labels.csv` 신설 (44건) + `agent/eval_real_labels.py` 신설** — 지금까지 `eval.py`는 자작 5문서만 평가했는데(정작 이번 세션에 그렇게 많이 모은 hldcc/LBox/AI Hub 골든데이터는 전혀 안 쓰이고 있었음), real_labels.md 4건 + hldcc 14건(id=24 포함) + LBox 13건(90237의 5개 포함) + AI Hub 13건을 조항 단위 정답지로 통합. 이 데이터는 완전한 계약서가 아니라 판결문/조정문에서 이미 추출된 개별 조항이라 Parser가 필요 없음 — `analysis._analyze_clause()`를 조항 단위로 직접 호출해 risk_level 이진판정(TP/FP/FN/TN/precision/recall) + risk_type 혼동행렬 + 불일치 사례를 `docs/eval_real_labels.md`로 저장. **미실행**(API 키 필요, `eval_pairwise.py`와 함께 실행 대상)

**사람 평가**
- ✅ **설계 재작업 완료**: `docs/human_llm_judge_agreement_design.md` 11절 — 비교/선호 판단(선호일치율·Cohen's κ·**정답 일치율**)을 주 실험으로 채택, 기존 QWK 설계는 실시간 게이팅 검증용 보조 트랙으로 격하
- ✅ 정답앵커 쌍 10건 제작 → `data/anchor_pairs.json` (위험 5/안전 5, risk_type 4종). 목표 19~25건에는 못 미침 — 실행 결과 보고 부족한 유형 위주 추가 권장
- ✅ `agent/eval_pairwise.py` 집계 스크립트 작성 완료 — **미실행**(API 호출 필요, 다음 세션에서 `python eval_pairwise.py` 실행)
- ⬜ 평가자 섭외 (팀 내부 2인 + 외부 평가자 최소 1인)
- ⬜ 평가지(구글폼) 제작 — 절대점수 섹션 + pairwise 섹션 둘 다 포함, `anchor_pairs.json`을 사람 평가용으로도 재사용
- ⬜ 캘리브레이션 라운드 → 본채점 → 통계분석 → 리포트

## 우선순위 3 — 프론트엔드 소소 개선 (김민찬 영역)

- ⬜ 왼쪽 조항 목록: "표준 조항" 일색 라벨 → 조항명/요약 노출 또는 위험 조항만 두드러지게
- ⬜ 오른쪽 상세: "쉬운 설명" 먼저 → "계약서 원문" 먼저 나오도록 순서 변경
- 시간 남으면 진행, 우선순위상 데이터 작업보다 낮음

## 우선순위 4 — 백엔드 인프라 (후순위로 보류)

- ⚠️ **확인 필요**: JWT/MySQL/비동기/Docker 전체가 후순위인지, `analyze-pdf` 프록시처럼 데모 자체 동작에 필요한 P0 항목(`docs/backend_spec.md`)까지 포함되는지 교수님께 재확인
- 확인 후 `backend/README.md`·`docs/backend_spec.md` TODO 우선순위표 갱신

## 우선순위 5 — RAG (신중, 후순위)

- ⬜ 관찰1이 정리된 뒤, 스코프를 무리하지 않는 선에서 검토만

## 우선순위 6 — 전세사기 구조적 위험 탐지 확장 (신중, 팀 논의 필요)

`docs/jeonse_fraud_causes_research.md` 리서치에서 나온 진짜 스코프 확장 후보들 — 조항 텍스트
분석을 넘어서는 신규 백엔드 기능이라 교수님의 "지금은 검증 우선, 시스템 완성은 후순위"
피드백과 정면으로 충돌할 수 있음. **진행 여부는 팀/교수님과 먼저 논의**.

- ⬜ 국토교통부 실거래가 공개시스템 API 연동 → 전세가율(보증금/시세) 자동 계산 및 경고
- ⬜ 등기부등본 업로드·파싱 → 다주택 여부, 근저당/신탁 여부 자동 확인
- ⬜ 한국공인중개사협회 자격조회 연동 → 중개사 정상 등록 여부 확인

---

## 지금 바로 할 수 있는 것 (의존성 없음, 저비용)

이 중 몇 개는 다음 세션에서 바로 처리 가능:
1. `judge.py` aspect 순서 랜덤화 (코드 3줄)
2. `judge.py` rationale 저장 (state.py 필드 추가 + print 대체)
3. `eval.py` ↔ `clause_level_labels.csv` 조인해서 정밀 recall/precision 계산
4. 사람 평가 설계를 선호 비교(pairwise) 방식으로 재작성

## 팀 논의가 필요한 것 (내가 혼자 결정 못함)

1. ~~AI Hub 신청자/일정~~ → **진행하는 것으로 결정**, 신청자/일정만 남음
2. ~~전세사기 데이터 사용 여부~~ → **사용하는 것으로 결정**, risk_type 확장("신탁관계·소유권 불안정 고지") 정식 채택 여부만 남음
3. LBox 43MB 파일 git 관리 방식
4. 백엔드 P0(analyze-pdf 프록시)가 후순위에 포함되는지 교수님 확인
5. 외부 평가자(사람 annotation) 섭외 대상
